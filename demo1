// ignore 
// to do: decisions and states can be combinatorials, hierarchies, 
// can be referred to as all(), starts_with(), ... 
// to do: custom functions 
// to do: build-in funcitons: e.g., prob2rate, ... 
// to do: build-in variables: e.g., age, cycle

global parameters { // 
  n_cycles = 40;
}

decisions { // can be combinatorialchoices or alternatives one per line
  No trt;
  Treatment A;
  Treatment B;
}

health states { // where people spend time
  Moderate;
  Severe;
  Dead;
}

transitions { // can be function of cycle or tau

}